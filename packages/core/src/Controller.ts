export abstract class Controller {
    // Shared logic for controllers should go here (middleware, helpers, etc.)
    public show () { }
    public index () { }
    public store () { }
    public update () { }
    public destroy () { }
}
